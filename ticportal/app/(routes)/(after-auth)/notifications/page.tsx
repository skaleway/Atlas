import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Notifications",
};

const Notification = () => {
  return (
    <main className="">
      <h1>Notification</h1>
    </main>
  );
};

export default Notification;
