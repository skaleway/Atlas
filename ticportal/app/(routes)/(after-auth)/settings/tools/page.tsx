import { Metadata } from "next";

import { currentProfile } from "@/lib/current-profile";

export const metadata: Metadata = {
  title: "Tools Settings",
};

const ToolsSettings = () => {
  return <div>ToolsSettings</div>;
};

export default ToolsSettings;
