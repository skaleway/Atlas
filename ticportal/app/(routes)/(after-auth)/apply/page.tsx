import GlobalText from "@/components/shared/text";
import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Apply",
};

const Apply = () => {
  return (
    <div>
      <GlobalText>Programs - apply</GlobalText>
    </div>
  );
};

export default Apply;
